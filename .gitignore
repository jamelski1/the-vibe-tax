__pycache__/
*.pyc

# Logs
*.log

# Generated run / scoring artifacts (reproducible from the scripts; the
# committed *_scored_stats.json / *_stats.json aggregates are enough)
vibe_tax_test_results.json
vibe_tax_test_stats.json
vibe_tax_progress.json
vibe_tax_run_stats.json

# Third-party benchmark data (reproducible; download cmd in score_vibe_tax_plus.py)
HumanEvalPlus.jsonl
*.jsonl.gz


# LCB generator output (regenerated per backend) + local scoring data
lcb_v3_prompts.json
lcb_test_prompts.json
lcb_tests.jsonl
lcb_progress.json
lcb_v3_responses_3model.json

# HE4 (4-condition HumanEval) local run artifacts — need API keys; the
# committed he4_prompts.json (deterministic) + he4_scored_stats.json aggregate
# are enough to reproduce/inspect.
# he4_responses.json is intentionally committed (used by build_breakdown.py)
he4_progress.json
he4_run_stats.json
